# Grafana admin 凭据模板（P3）。真值不落库。
#
# kubectl apply -f <填好真值的副本> -n platform
apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin
  namespace: platform
type: Opaque
stringData:
  admin-user: "admin"
  admin-password: "<random>"
