# P4 ClickHouse Secrets 模板（真值不落库）。
# 应用到 platform ns；chart 经 values clickhouse.yaml 的 auth.existingSecret 读取。
#
# kubectl apply -f <填好真值的副本> -n platform
apiVersion: v1
kind: Secret
metadata:
  name: clickhouse-admin
  namespace: platform
type: Opaque
stringData:
  password: "change-me-please"
