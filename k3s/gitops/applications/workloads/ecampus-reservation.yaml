apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ecampus-reservation
  namespace: argocd
spec:
  project: workloads
  source:
    repoURL: http://gitea-http.delivery.svc.cluster.local:3000/gitea_admin/app-test.git
    targetRevision: main
    path: k3s/charts/go-service
    helm:
      releaseName: ecampus-reservation
      valueFiles:
        - ../../helm-values/workloads/ecampus-reservation.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: app
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
