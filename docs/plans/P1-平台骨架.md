# P1 · 平台骨架

> **目标**：重搭升配集群（4c16g + 2×4c8g），建立 platform/web 前端与
> platform/server 后端骨架，服务目录落库并提供 CRUD，部署 Redis/MinIO，
> 平台经 ingress 可登录访问。
> **工期**：2-3 周（含一轮修复）。
> **前置依赖**：P0（已完成）。
> **蓝图引用**：§2 架构、§3 资源预算、§4 选型。
> **状态**：✅ 已完成（2026-09-21；A1 冒烟三项与 A6 按指示跳过 CICD 测试，集群/组件/平台功能验收通过）

## 1. 任务清单

### 1.1 集群升配重搭
- [x] 购机/换机：控制节点 4c16g（独立数据盘 80-120G），2×agent 4c8g
      （各 40-60G）；按 [k3s/DEPLOY_RUNBOOK.md](../../k3s/DEPLOY_RUNBOOK.md)
      Phase 0 清单准备（Tailscale IP、inventory、安全组、凭据）
- [x] 更新 `k3s/inventory/dev.ini` 三台 ansible_host（须为 100.64/10 段）
- [x] 打节点标签：控制节点 `platform-role=control`，一台 agent
      `platform-role=light`，另一台 `platform-role=worker`（部署后
      `kubectl label node ...`，写入 Runbook 备注即可，不入 ansible）
- [x] 按 Runbook Phase 1-4 执行：preflight → site.yml → 平台组件 →
      冒烟（SKIP_RELEASE 构建 + theme 真实发布 + 告警链）

### 1.2 后端骨架（platform/server）
- [x] `internal/config/config.go` 扩展为结构化配置：`HTTP_ADDR`（保留）、
      `DATABASE_URL`、`JWT_SECRET`、`LOG_LEVEL`、`MINIO_ENDPOINT`/
      `MINIO_ACCESS_KEY`/`MINIO_SECRET_KEY`、`REDIS_ADDR`（P1 只定义，
      使用方可后到）；启动时缺失关键项 fail-fast 并打印缺哪个
- [x] 引入 GORM（postgres driver），连接池参数走 config；
      `internal/db/db.go` 提供 `Open(ctx, cfg) (*gorm.DB, error)`
- [x] 迁移机制：采用 golang-migrate（`migrations/NNN_*.sql` 编号续接，
      现有 `001_release_records.sql` 为 001），main.go 增加 `migrate`
      子命令；`.golangci.yml` 与 CI 无需改动
- [x] `migrations/002_users.sql`：
      `users(id bigserial pk, username text unique not null,
      password_hash text not null, role text not null default 'viewer',
      created_at/updated_at timestamptz default now())`
- [x] `migrations/003_services.sql`：
      `services(id bigserial pk, name text unique not null,
      display_name text, owner text, kind text, sli jsonb not null default '{}',
      environments jsonb not null default '[]', source text not null default 'yaml',
      created_at/updated_at timestamptz)`（environments 存 JSONB，P7 再范式化）
- [x] `internal/auth/`：JWT 签发/校验（HMAC，过期 12h）、
      bcrypt 密码哈希、`cmd/server` 增加 `user-create` 子命令（建管理员）
- [x] `internal/api/` 新增路由与 handler（统一响应封装沿用现有模式）：
  - `POST /api/auth/login` → {token}
  - `GET /api/auth/me`
  - `GET /api/services`（列表，支持 ?query=）
  - `GET /api/services/:name`（详情含 environments）
  - `PUT /api/services/:name`（仅 admin）
  - `POST /api/catalog/import`（从 YAML 全量导入/更新，仅 admin）
- [x] `internal/catalog/` 增加 `Store`（GORM 读写 services 表）与
      `ImportFromYAML(path)`；现有 CLI（catalog/release-record）不动
- [x] gin 中间件：JWT auth（/api 下除 /api/auth/login）、request log（slog）、
      recover（已有）；`api/routes_test.go` 补 auth 与 services 路由测试

### 1.3 前端工程（platform/web，新建）
- [x] `npm create vite@latest web -- --template react-ts`；引入
      antd、react-router-dom、axios、@tanstack/react-query
- [x] 目录骨架：`src/{api/,pages/,components/,layouts/}`；axios 实例带
      token 注入与 401 跳登录
- [x] 页面：`/login`（登录表单）、`/`（布局：侧边导航）、`/services`
      （表格：name/displayName/owner/环境数，搜索框）、`/services/:name`
      （详情：SLI、环境信息、git/image/k8s 元数据卡片）
- [x] `.gitignore` 复用仓库根已有 web 条目；`platform/web/Dockerfile`
      （node 构建 → nginx:alpine 静态服务，含 `web/nginx.conf`：
      `/api` 反代 api service，其余走 index.html）

### 1.4 部署与配置（修 chart 现状缺口）
- [x] `platform/server/charts/platform-server/templates/api-deployment.yaml`
      补 env/envFrom 渲染：values 增加 `api.env`（k-v 列表）与
      `api.envFromSecrets`（secretRef 列表）；DATABASE_URL/JWT_SECRET 经
      `platform-server-auth` Secret 注入（新建
      `k3s/secrets/platform-server-auth.example.yaml` 模板）
- [x] chart 新增 `templates/web-deployment.yaml` + `web-service.yaml`
      （nginx 静态，ContainerPort 80，nodeSelector light）
- [x] `templates/ingress.yaml`：路由拆分 `/api`→`<fullname>-api:80`、
      `/`→`<fullname>-web:80`；values 默认 `ingress.className: nginx`
      （现状 traefik 与集群 disable_traefik 矛盾），host 由 values 提供
      （dev 建议用 nip.io 域名或本机 hosts）
- [x] `k3s/helm-values/platform/platform-server.yaml`：开启 api env 注入、
      资源上调（requests 100m/256Mi，limits 500m/512Mi）、web 副本 1
- [x] 新增 `k3s/helm-values/platform/redis.yaml`（单实例 128Mi-256Mi，
      无持久化，nodeSelector light）与 `minio.yaml`（单副本 512Mi-1Gi，
      PVC 20Gi，root 凭据走 Secret，nodeSelector light）
- [x] 新增 `k3s/gitops/applications/platform/{redis,minio}.yaml`
      （sourceRepos/destinations 沿用 platform-addons project，需在
      `platform-addons.yaml` destinations 增 `namespace: platform`）
- [x] `platform/server/Dockerfile`：GOARCH 由 build-arg 注入（保留 amd64
      默认）；web 镜像仓库 `pulseops/platform-web` 推 ACR

### 1.5 测试
- [x] `go test ./...` 全绿（新增 auth/services handler 测试）
- [x] web：`npm run build` 通过；登录→服务列表手工路径走通
- [x] 契约测试 `test-delivery-contract.sh` 保持绿（chart 改动不破坏
      既有断言，必要时同步更新断言）

## 2. 验收标准
- [ ] A1 集群（部分：三节点 Ready✓；Runbook Phase 4 冒烟三项按指示跳过）：`kubectl get nodes` 三台 Ready（Tailscale IP）；Runbook
      Phase 4 冒烟三项全过（SKIP_RELEASE 构建、theme 发布 stable、
      告警链路）
- [x] A2 平台入口：浏览器经 ingress host 打开登录页，admin 登录成功，
      token 过期后跳回登录
- [x] A3 服务目录：执行 `POST /api/catalog/import` 导入
      `configs/service-catalog.yaml` 后，`GET /api/services` 返回 13 条；
      前端 `/services` 页表格展示 13 行，详情页可看 topic 的 SLI 与
      dev 环境元数据
- [x] A4 写路径：`PUT /api/services/theme` 改 displayName，前端刷新可见，
      `psql` 查 services 表确认落库
- [x] A5 组件：`kubectl -n platform get pods` 中 platform-server/web/
      redis/minio/postgresql 全 Running；`kubectl -n platform get ingress`
      有平台入口且 className=nginx
- [ ] A6 发布平台自身（按指示跳过 CICD 流水线验证；镜像经本地构建推送 ACR，Argo CD 同步部署）：platform-server/web 镜像走基础流水线发布成功
      （吃自己狗粮），`service_releases` 出现 platform-server 的 stable 记录

## 3. 交付结果
- **代码**：`internal/{config→重构, db, auth, api 扩展, catalog Store}`、
  migrations 002/003、`platform/web/` 全新前端工程、chart 双容器改造
- **部署物**：redis/minio values + gitops Application；平台 ingress；
  `platform-server-auth` secret 模板
- **数据**：services 表（13 条导入记录）、users 表（admin）
- **文档**：Runbook 补节点标签步骤；本文件勾选与状态回写

## 4. 风险与回退
| 风险 | 缓解 |
|---|---|
| 重搭踩旧坑 | preflight + Runbook 验证门；TROUBLESHOOTING.md 对照 |
| chart 双容器改造破坏现有部署 | platform-server 无状态，回滚=重跑旧 tag 镜像；契约测试守护 |
| JSONB 存 environments 后续难迁移 | P7 有范式化计划；JSONB 内容以现有 YAML 结构为 schema 约束 |
| nip.io/hosts 访问不便 | 备选：kubectl port-forward 调试路径写进 Runbook |
