# 三台阿里云 k3s 集群：部署 + 简历亮点逐条实证计划（最终版 · 输入齐备）

## 目标
三台服务器作为验证场：简历四条亮点各自声称解决的问题，逐条用可复现实验证明"真的能解决"，产出量化数字、证据链（benchmarks/evidence/）、排障案例与架构讲稿。授权条款：验证中发现设计与声明不符，小修直接改、大改先报方案，全程维持代码=文档=简历一致。凭据（ACR 密码、GitHub PAT）只入集群 Secret/运行时环境，绝不落 git。

## 输入台账（全部到位）
- node3（4c16g, 100.83.57.72）= k3s server + control；node1（2c4g, 100.80.235.120）、node2（2c4g, 100.99.235.70）= agent（其一 light）；全部 ubuntu，密钥 ~/.ssh/k3s_mac_ed25519；Mac 与三台同 tailnet → inventory 全用 100.x，roles 零改动
- 源码仓库 worryyy/app-test（public）、GitOps 仓库 worryyy/devops-test（public + 分支保护/PR 门禁）
- ACR：crpi-gfwwpdquc14b7w22(.vpc).cn-shanghai.personal.cr.aliyuncs.com，命名空间 pulseops（自动建仓已开），凭据已提供
- GitHub PAT 已提供（fine-grained，用于 git-https Jenkins 凭据：克隆/推分支 + 建 PR/auto-merge API）

## 执行阶段
1. **连通性探测**：Mac→三台 100.x ping/SSH、三向互通、地域 metadata、磁盘核对（server 需 ≥100G）
2. **app-test 内容验证**：clone 检查 cmd/ecampus-impact、13 服务目录、build/Dockerfile.go-service；缺失则从 Ecampus-go 镜像补推
3. **ACR 切换 + 必修项**（本地改码推 devops-test）：Jenkinsfile（BUILDKIT_CACHE_REPO→pulseops/buildkit-cache、CI Pod 六容器补 requests/limits）；service-catalog 与 workloads values ×13 镜像路径切 ACR（VPC/公网按地域定，前缀抽单处变量）；tcr-secret/tcr-kaniko-secret 换 ACR 凭据；ecampus-theme prune 对齐 false；rollback-release.sh 插入"回退前暂停目标 Application 同步、补偿后恢复"；Jenkins controller 等镜像 mirror 到 pulseops（服务器 ctr 完成）
4. **集群搭建**：inventory 重写（node3=server）→ ansible site.yml → 标签 control/light/worker → secrets → install-argocd.sh + gitops projects/applications → PG 迁移 → 等 19 Application Synced/Healthy
5. **V0 冒烟**：Jenkins 任务（SCM=devops-test 的 Jenkinsfile，凭据 git-https=用户名+PAT）→ 不带 SHA 全量发布端到端绿 → PG stable 记录 → hey 常驻流量（≥3.3rps）
6. **V1 亮点一实证**：阶段瀑布分解（新脚本解析 Jenkins 时间戳+Argo sync+Pod Ready，定位耗时根因）+ 递进消融 L0 全冷全量→L1 +层缓存→L2 +Go 缓存→L3 +影响裁剪，每级 ×2~3 取中位；影响分析真实性（单服务提交→1 镜像；共享库→闭包扩展；Dockerfile→边界记录）
7. **V2 亮点四实证**：with/without 抑制规则 A/B（GitOps PR 移除 inhibit_rules → 同场景重测 → revert，全程可审计），3 轮均值 + inject-restart/inject-crashloop 安全断言（持续类 0 抑制）
8. **V3 亮点三实证**：坏提交（健康/路由 500）触发自动回退 → 先观察 selfHeal=true 是否把失败版本 heal 回集群（大概率中招=真实排障案例）→ 应用 rollback 同步暂停修复 → 重演四断言（Argo 不再部署失败版本/流量回 stable digest/GitOps main 回上一版/PG 一致）+ 回退耗时
9. **V4/V5 收尾**：排障案例按模板入 TROUBLESHOOTING_STORY、架构一页版刷新 ARCHITECTURE_SCRIPT、全部数字回填简历占位符、evidence 目录入库