version: v1

services:
  - name: academic
    displayName: Ecampus Academic
    owner: worryyy
    kind: go-service
    sli: {requestRouteRegex: '^/api/academic.*$', maxP95Seconds: 0.8}
    environments:
      - name: dev
        namespace: app
        branchPolicy: &mainBranch {defaultBranch: main, allowedBranches: [main]}
        git: {repo: https://github.com/worryyy/app-test.git, chartPath: k3s/charts/go-service, valuesFile: k3s/helm-values/workloads/ecampus-academic.yaml}
        image: {repository: crpi-gfwwpdquc14b7w22.cn-shanghai.personal.cr.aliyuncs.com/pulseops/ecampus-academic, tagPolicy: git-sha, requireDigest: true}
        jenkins: &jenkins {mode: webhook, jobName: ecampus-pipeline}
        argocd: {application: ecampus-academic, namespace: argocd}
        kubernetes: {namespace: app, workload: ecampus-academic, service: ecampus-academic, container: ecampus-academic}
        health: &health {healthPath: /health, readyPath: /health}

  - name: agentchat
    displayName: Ecampus Agent Chat
    owner: worryyy
    kind: go-service
    sli: {requestRouteRegex: '^/api/agent.*$', maxP95Seconds: 1.5}
    environments:
      - name: dev
        namespace: app
        branchPolicy: *mainBranch
        git: {repo: https://github.com/worryyy/app-test.git, chartPath: k3s/charts/go-service, valuesFile: k3s/helm-values/workloads/ecampus-agentchat.yaml}
        image: {repository: crpi-gfwwpdquc14b7w22.cn-shanghai.personal.cr.aliyuncs.com/pulseops/ecampus-agentchat, tagPolicy: git-sha, requireDigest: true}
        jenkins: *jenkins
        argocd: {application: ecampus-agentchat, namespace: argocd}
        kubernetes: {namespace: app, workload: ecampus-agentchat, service: ecampus-agentchat, container: ecampus-agentchat}
        health: *health

  - name: chat
    displayName: Ecampus Chat
    owner: worryyy
    kind: go-service
    sli: {requestRouteRegex: '^/api/(conversation|message).*$', maxP95Seconds: 1.0}
    environments:
      - name: dev
        namespace: app
        branchPolicy: *mainBranch
        git: {repo: https://github.com/worryyy/app-test.git, chartPath: k3s/charts/go-service, valuesFile: k3s/helm-values/workloads/ecampus-chat.yaml}
        image: {repository: crpi-gfwwpdquc14b7w22.cn-shanghai.personal.cr.aliyuncs.com/pulseops/ecampus-chat, tagPolicy: git-sha, requireDigest: true}
        jenkins: *jenkins
        argocd: {application: ecampus-chat, namespace: argocd}
        kubernetes: {namespace: app, workload: ecampus-chat, service: ecampus-chat, container: ecampus-chat}
        health: *health

  - name: comment
    displayName: Ecampus Comment
    owner: worryyy
    kind: go-service
    sli: {requestRouteRegex: '^/api/comment.*$', operationRouteRegex: '^/api/comment/:topic_id$', maxP95Seconds: 0.5}
    environments:
      - name: dev
        namespace: app
        branchPolicy: *mainBranch
        git: {repo: https://github.com/worryyy/app-test.git, chartPath: k3s/charts/go-service, valuesFile: k3s/helm-values/workloads/ecampus-comment.yaml}
        image: {repository: crpi-gfwwpdquc14b7w22.cn-shanghai.personal.cr.aliyuncs.com/pulseops/ecampus-comment, tagPolicy: git-sha, requireDigest: true}
        jenkins: *jenkins
        argocd: {application: ecampus-comment, namespace: argocd}
        kubernetes: {namespace: app, workload: ecampus-comment, service: ecampus-comment, container: ecampus-comment}
        health: *health

  - name: file
    displayName: Ecampus File
    owner: worryyy
    kind: go-service
    sli: {requestRouteRegex: '^/file.*$', maxP95Seconds: 1.5}
    environments:
      - name: dev
        namespace: app
        branchPolicy: *mainBranch
        git: {repo: https://github.com/worryyy/app-test.git, chartPath: k3s/charts/go-service, valuesFile: k3s/helm-values/workloads/ecampus-file.yaml}
        image: {repository: crpi-gfwwpdquc14b7w22.cn-shanghai.personal.cr.aliyuncs.com/pulseops/ecampus-file, tagPolicy: git-sha, requireDigest: true}
        jenkins: *jenkins
        argocd: {application: ecampus-file, namespace: argocd}
        kubernetes: {namespace: app, workload: ecampus-file, service: ecampus-file, container: ecampus-file}
        health: *health

  - name: marketplace
    displayName: Ecampus Marketplace
    owner: worryyy
    kind: go-service
    sli: {requestRouteRegex: '^/api/marketplace.*$', maxP95Seconds: 1.0}
    environments:
      - name: dev
        namespace: app
        branchPolicy: *mainBranch
        git: {repo: https://github.com/worryyy/app-test.git, chartPath: k3s/charts/go-service, valuesFile: k3s/helm-values/workloads/ecampus-marketplace.yaml}
        image: {repository: crpi-gfwwpdquc14b7w22.cn-shanghai.personal.cr.aliyuncs.com/pulseops/ecampus-marketplace, tagPolicy: git-sha, requireDigest: true}
        jenkins: *jenkins
        argocd: {application: ecampus-marketplace, namespace: argocd}
        kubernetes: {namespace: app, workload: ecampus-marketplace, service: ecampus-marketplace, container: ecampus-marketplace}
        health: *health

  - name: moderation
    displayName: Ecampus Moderation
    owner: worryyy
    kind: go-service
    sli: {requestRouteRegex: '^/api/moderation.*$', maxP95Seconds: 0.8}
    environments:
      - name: dev
        namespace: app
        branchPolicy: *mainBranch
        git: {repo: https://github.com/worryyy/app-test.git, chartPath: k3s/charts/go-service, valuesFile: k3s/helm-values/workloads/ecampus-moderation.yaml}
        image: {repository: crpi-gfwwpdquc14b7w22.cn-shanghai.personal.cr.aliyuncs.com/pulseops/ecampus-moderation, tagPolicy: git-sha, requireDigest: true}
        jenkins: *jenkins
        argocd: {application: ecampus-moderation, namespace: argocd}
        kubernetes: {namespace: app, workload: ecampus-moderation, service: ecampus-moderation, container: ecampus-moderation}
        health: *health

  - name: notification
    displayName: Ecampus Notification
    owner: worryyy
    kind: go-service
    sli: {requestRouteRegex: '^/api/(notification|notify).*$', maxP95Seconds: 0.8}
    environments:
      - name: dev
        namespace: app
        branchPolicy: *mainBranch
        git: {repo: https://github.com/worryyy/app-test.git, chartPath: k3s/charts/go-service, valuesFile: k3s/helm-values/workloads/ecampus-notification.yaml}
        image: {repository: crpi-gfwwpdquc14b7w22.cn-shanghai.personal.cr.aliyuncs.com/pulseops/ecampus-notification, tagPolicy: git-sha, requireDigest: true}
        jenkins: *jenkins
        argocd: {application: ecampus-notification, namespace: argocd}
        kubernetes: {namespace: app, workload: ecampus-notification, service: ecampus-notification, container: ecampus-notification}
        health: *health

  - name: reservation
    displayName: Ecampus Reservation
    owner: worryyy
    kind: go-service
    sli: {requestRouteRegex: '^/api/reservation.*$', maxP95Seconds: 1.0}
    environments:
      - name: dev
        namespace: app
        branchPolicy: *mainBranch
        git: {repo: https://github.com/worryyy/app-test.git, chartPath: k3s/charts/go-service, valuesFile: k3s/helm-values/workloads/ecampus-reservation.yaml}
        image: {repository: crpi-gfwwpdquc14b7w22.cn-shanghai.personal.cr.aliyuncs.com/pulseops/ecampus-reservation, tagPolicy: git-sha, requireDigest: true}
        jenkins: *jenkins
        argocd: {application: ecampus-reservation, namespace: argocd}
        kubernetes: {namespace: app, workload: ecampus-reservation, service: ecampus-reservation, container: ecampus-reservation}
        health: *health

  - name: school
    displayName: Ecampus School
    owner: worryyy
    kind: go-service
    sli: {requestRouteRegex: '^/api/(term|user/(authentication|re_authentication|get_course_by_weeks|get_exam|get_exam_score)).*$', maxP95Seconds: 2.0}
    environments:
      - name: dev
        namespace: app
        branchPolicy: *mainBranch
        git: {repo: https://github.com/worryyy/app-test.git, chartPath: k3s/charts/go-service, valuesFile: k3s/helm-values/workloads/ecampus-school.yaml}
        image: {repository: crpi-gfwwpdquc14b7w22.cn-shanghai.personal.cr.aliyuncs.com/pulseops/ecampus-school, tagPolicy: git-sha, requireDigest: true}
        jenkins: *jenkins
        argocd: {application: ecampus-school, namespace: argocd}
        kubernetes: {namespace: app, workload: ecampus-school, service: ecampus-school, container: ecampus-school}
        health: *health

  - name: theme
    displayName: Ecampus Theme
    owner: worryyy
    kind: go-service
    sli: {requestRouteRegex: '^/api/theme.*$', maxP95Seconds: 0.5}
    environments:
      - name: dev
        namespace: app
        branchPolicy: *mainBranch
        git: {repo: https://github.com/worryyy/app-test.git, chartPath: k3s/charts/go-service, valuesFile: k3s/helm-values/workloads/ecampus-theme.yaml}
        image: {repository: crpi-gfwwpdquc14b7w22.cn-shanghai.personal.cr.aliyuncs.com/pulseops/ecampus-theme, tagPolicy: git-sha, requireDigest: true}
        jenkins: *jenkins
        argocd: {application: ecampus-theme, namespace: argocd}
        kubernetes: {namespace: app, workload: ecampus-theme, service: ecampus-theme, container: ecampus-theme}
        health: *health

  - name: topic
    displayName: Ecampus Topic
    owner: worryyy
    kind: go-service
    sli: {requestRouteRegex: '^/api/(topic|like|collection).*$', operationRouteRegex: '^/api/topic$', maxP95Seconds: 0.5}
    environments:
      - name: dev
        namespace: app
        branchPolicy: *mainBranch
        git: {repo: https://github.com/worryyy/app-test.git, chartPath: k3s/charts/go-service, valuesFile: k3s/helm-values/workloads/ecampus-topic.yaml}
        image: {repository: crpi-gfwwpdquc14b7w22.cn-shanghai.personal.cr.aliyuncs.com/pulseops/ecampus-topic, tagPolicy: git-sha, requireDigest: true}
        jenkins: *jenkins
        argocd: {application: ecampus-topic, namespace: argocd}
        kubernetes: {namespace: app, workload: ecampus-topic, service: ecampus-topic, container: ecampus-topic}
        health: *health

  - name: user
    displayName: Ecampus User
    owner: worryyy
    kind: go-service
    sli: {requestRouteRegex: '^/api/(user|wx).*$', operationRouteRegex: '^/api/user/(login|refresh)$', maxP95Seconds: 0.5}
    environments:
      - name: dev
        namespace: app
        branchPolicy: *mainBranch
        git: {repo: https://github.com/worryyy/app-test.git, chartPath: k3s/charts/go-service, valuesFile: k3s/helm-values/workloads/ecampus-user.yaml}
        image: {repository: crpi-gfwwpdquc14b7w22.cn-shanghai.personal.cr.aliyuncs.com/pulseops/ecampus-user, tagPolicy: git-sha, requireDigest: true}
        jenkins: *jenkins
        argocd: {application: ecampus-user, namespace: argocd}
        kubernetes: {namespace: app, workload: ecampus-user, service: ecampus-user, container: ecampus-user}
        health: *health
